version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7000:7000"
    volumes:
      - ./src:/app/src
    networks:
      - distributed-network

networks:
  distributed-network:
    driver: bridge